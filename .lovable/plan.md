No code changes required.

The full design system reference (fonts, sizes, weights, HSL color tokens, sidebar/topbar/card/input/button/table specs, and SAP API screen styling) was provided above in the chat as requested.

If you later want this captured as a file in the repo (e.g. `docs/DESIGN_SYSTEM.md`) or as an interactive `/design-system` showcase page, approve this plan and tell me which format — I will add it then.